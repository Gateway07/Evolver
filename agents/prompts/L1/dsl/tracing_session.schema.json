{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/schemas/tracing_session.schema.json",
  "title": "Tracing Session Output (Wrapper-collected)",
  "type": "object",
  "additionalProperties": false,
  "required": ["tracing_id", "breakpoints", "tracings"],
  "properties": {
    "tracing_id": { "type": "string", "maxLength": 128 },
    "breakpoints": {
      "description": "Configured tracing breakpoints for the run. Each BreakPoint can contain nested thenPoints (recursive) to represent conditional sub-traces that should only be captured when the parent BreakPoint was matched and its ifConditionExpression evaluated to true.",
      "type": "array",
      "minItems": 0,
      "maxItems": 16,
      "items": { "$ref": "#/$defs/breakPoint" }
    },
    "tracings": {
      "description": "Collected runtime tracing records for the run. Entries can be nested via thenTracings to reflect the runtime nesting caused by matching a BreakPoint with thenPoints.",
      "type": "array",
      "minItems": 0,
      "maxItems": 100000,
      "items": { "$ref": "#/$defs/tracing" }
    }
  },
  "$defs": {
    "spelExpression": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Spring Expression Language (SpEL) expression. The evaluation context uses: root object = 'this' (the intercepted instance); variables: method parameters by name; 'return' is available for AFTER expressions (method result); 'throw' is available for ON_ERROR expressions (exception). The evaluated value is serialized using the configured Options (container size limits, object field representation, and max depth)."
    },
    "representation": {
      "type": "string",
      "enum": ["PUBLIC_FIELDS", "DECLARED_FIELDS", "DECLARED_FIELDS_WITH_GETTERS"],
      "description": "Object field extraction mode used when building a JSON-safe representation of an object. PUBLIC_FIELDS: only public fields; DECLARED_FIELDS: all declared fields; DECLARED_FIELDS_WITH_GETTERS: declared fields plus getter-derived values (if supported by the adapter)."
    },
    "options": {
      "type": "object",
      "additionalProperties": false,
      "required": ["maxDepthLimit"],
      "properties": {
        "sizeLimitPerContainerClass": {
          "type": ["object", "null"],
          "description": "Container/array size limiting rules used when rendering values. Key: fully-qualified Java class name of the container type (e.g. java.util.List, java.util.Map). Value semantics: if a class key is absent, the adapter defaults to limiting that container class to 10 elements/entries; if present and value < 0, traversal stops for that container (it is not expanded); if present and value >= 0, expansion is limited to that many elements/entries.",
          "additionalProperties": { "type": "integer" }
        },
        "objectRepresentationPerClass": {
          "type": ["object", "null"],
          "description": "Overrides for how specific object types are represented. Key: fully-qualified Java class name. Value: representation mode. If null or empty, the adapter uses its default object rendering strategy.",
          "additionalProperties": { "$ref": "#/$defs/representation" }
        },
        "maxDepthLimit": {
          "type": "integer",
          "minimum": 0,
          "maximum": 64,
          "description": "Maximum traversal depth for object graphs when converting values to a JSON-safe representation. Once the depth limit is reached, nested values are truncated/stopped to avoid runaway recursion and excessive output."
        }
      }
    },
    "breakPoint": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "className",
        "lineOfCode",
        "watchExpressionsBefore",
        "watchExpressionsAfter",
        "watchExpressionsOnError",
        "ifConditionExpression",
        "thenPoints",
        "options"
      ],
      "properties": {
        "className": {
          "type": "string",
          "minLength": 1,
          "maxLength": 512,
          "description": "Fully-qualified class name where the breakpoint applies."
        },
        "lineOfCode": {
          "type": "integer",
          "minimum": 1,
          "maximum": 2000000000,
          "description": "Line number in the source file. Matching is tolerant (within ±1 line) to account for source/bytecode line mapping differences."
        },
        "watchExpressionsBefore": {
          "type": ["array", "null"],
          "maxItems": 256,
          "items": { "$ref": "#/$defs/spelExpression" },
          "description": "SpEL expressions evaluated before the method proceeds."
        },
        "watchExpressionsAfter": {
          "type": ["array", "null"],
          "maxItems": 256,
          "items": { "$ref": "#/$defs/spelExpression" },
          "description": "SpEL expressions evaluated after the method returns successfully. Variable 'return' is available."
        },
        "watchExpressionsOnError": {
          "type": ["array", "null"],
          "maxItems": 256,
          "items": { "$ref": "#/$defs/spelExpression" },
          "description": "SpEL expressions evaluated when the method throws. Variable 'throw' is available."
        },
        "ifConditionExpression": {
          "type": ["string", "null"],
          "maxLength": 2000,
          "description": "Optional SpEL boolean expression used to decide whether this breakpoint's tracing flow is active. If null/blank, the breakpoint always passes. If evaluation fails, it is treated as false."
        },
        "thenPoints": {
          "type": ["array", "null"],
          "maxItems": 16,
          "items": { "$ref": "#/$defs/breakPoint" },
          "description": "Recursive nested breakpoints. When this BreakPoint matches and passes ifConditionExpression, thenPoints become eligible for matching during the lifetime of the current scope, allowing nested (then) tracing trees."
        },
        "options": {
          "$ref": "#/$defs/options",
          "description": "Rendering options used when serializing watched values for this breakpoint."
        }
      }
    },
    "phase": {
      "type": "string",
      "enum": ["CALL", "ERROR"],
      "description": "Tracing record phase. CALL indicates a normal call tracing record; ERROR indicates an error-path record."
    },
    "tracing": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "phase",
        "point",
        "lineOfCode",
        "ifCondition",
        "watchesBefore",
        "watchesAfter",
        "watchesOnError",
        "stackTrace",
        "errorMessage",
        "timestampMs",
        "durationMs",
        "thenTracings"
      ],
      "properties": {
        "phase": { "$ref": "#/$defs/phase" },
        "point": {
          "type": "string",
          "minLength": 1,
          "maxLength": 512,
          "description": "Tracing point identifier (typically the class name)."
        },
        "lineOfCode": {
          "type": "integer",
          "minimum": 1,
          "maximum": 2000000000
        },
        "ifCondition": {
          "type": "boolean",
          "description": "Whether the breakpoint's ifConditionExpression evaluated to true for this record (i.e., whether the tracing flow was active)."
        },
        "watchesBefore": {
          "type": ["object", "null"],
          "description": "Map from watch expression string to its rendered value (or evaluation error message).",
          "additionalProperties": { "type": "string" }
        },
        "watchesAfter": {
          "type": ["object", "null"],
          "description": "Map from watch expression string to its rendered value (or evaluation error message).",
          "additionalProperties": { "type": "string" }
        },
        "watchesOnError": {
          "type": ["object", "null"],
          "description": "Map from watch expression string to its rendered value (or evaluation error message).",
          "additionalProperties": { "type": "string" }
        },
        "stackTrace": {
          "type": ["string", "null"],
          "maxLength": 200000,
          "description": "Stack trace captured when an exception is thrown (null if no exception)."
        },
        "errorMessage": {
          "type": ["string", "null"],
          "maxLength": 20000,
          "description": "Exception message captured when an exception is thrown (null if no exception)."
        },
        "timestampMs": {
          "type": "integer",
          "minimum": 0,
          "description": "Record timestamp in milliseconds since epoch."
        },
        "durationMs": {
          "type": "integer",
          "minimum": 0,
          "description": "Duration of the intercepted call in milliseconds."
        },
        "thenTracings": {
          "type": ["array", "null"],
          "maxItems": 10,
          "items": { "$ref": "#/$defs/tracing" },
          "description": "Nested tracing records produced by matching thenPoints within the open scope created by this breakpoint."
        }
      }
    }
  }
}
