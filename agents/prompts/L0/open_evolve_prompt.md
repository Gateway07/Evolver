# Counterexample Discovery + Theory Modeling (Address Search)

Execute below PromptCode to maximize **NotFound counterexamples** rate in a core address-search Java application by
discovering valid SQL WHERE-suffixes which should be tested and traced by the evaluator to understand the search process
and identify potential sources of errors/bugs.

```code

<PromptCode>
</PromptCode>
```